# XQuant Prediction System

ระบบทำนายความเข้มข้นแบบ Real Time และ Normal สำหรับ XQuant Colorimetric

## 🚀 คุณสมบัติหลัก

### 1. **Real Time Analysis**
- วัดความเข้มข้นแบบ Real Time
- ตั้งค่าความเข้มข้นเป้าหมายและค่าความคลาดเคลื่อน (±)
- แสดงสถานะสี:
  - 🟢 **สีเขียว**: ถึงเป้าหมาย
  - 🔴 **สีแดง**: เกินเป้าหมาย
  - 🟡 **สีเหลือง**: ต่ำกว่าเป้าหมาย
- อัพเดทผลการวัดทุกวินาที

### 2. **Normal Prediction**
- ทำนายความเข้มข้นจาก Absorbance
- ทำนายความเข้มข้นจากค่า RGB
- แสดงผลการทำนายพร้อมความแม่นยำ

### 3. **การโหลดข้อมูล**
- โหลดข้อมูลจาก Local Storage
- โหลดข้อมูลจาก Server (พร้อม backup)
- โหลดข้อมูลจาก Session Storage

## 📁 ไฟล์ที่เกี่ยวข้อง

```
4/
├── prediction.html          # หน้า Prediction หลัก
├── prediction-styles.css    # CSS สำหรับ Prediction
├── prediction-script.js     # JavaScript สำหรับ Prediction
├── index.html              # หน้าหลัก (มีปุ่มไป Prediction)
├── script.js               # JavaScript หน้าหลัก
└── DATA_STRUCTURE.json     # โครงสร้างข้อมูล JSON
```

## 🎯 วิธีการใช้งาน

### ขั้นตอนที่ 1: สร้างสมการในหน้าหลัก
1. เปิด `index.html`
2. ป้อนข้อมูลตัวอย่าง (Samples)
3. สร้างสมการเชิงเส้น
4. บันทึกข้อมูล (Local/Server/Session)

### ขั้นตอนที่ 2: ไปยังหน้า Prediction
1. กดปุ่ม "ไปหน้า Prediction" ในหน้าหลัก
2. หรือเปิด `prediction.html` โดยตรง

### ขั้นตอนที่ 3: โหลดข้อมูล
1. เลือกประเภทข้อมูล (Local/Server/Session)
2. เลือกข้อมูลที่ต้องการใช้
3. ระบบจะแสดงสมการที่โหลด

### ขั้นตอนที่ 4: เลือกโหมดการทำงาน

#### **Real Time Analysis**
1. เลือกโหมด "Real Time Analysis"
2. ป้อนความเข้มข้นเป้าหมาย (เช่น 6.0)
3. ตั้งค่าความคลาดเคลื่อน (เช่น ±0.5)
4. กด "เริ่มการวัด"
5. ดูผลการวัดแบบ Real Time

#### **Normal Prediction**
1. เลือกโหมด "Normal Prediction"
2. เลือกวิธีการป้อนข้อมูล:
   - **Absorbance**: ป้อนค่า Absorbance โดยตรง
   - **RGB**: ป้อนค่า RGB (0-255)
3. กด "ทำนายความเข้มข้น"
4. ดูผลการทำนาย

## 🔧 ฟีเจอร์ทางเทคนิค

### Real Time Simulation
- จำลองการวัดแบบ Random Walk
- คำนวณ Absorbance จากความเข้มข้นเป้าหมาย
- แสดงสถานะตามค่าความคลาดเคลื่อน

### การคำนวณ
- **Absorbance → Concentration**: ใช้สมการ y = mx + b
- **RGB → Absorbance**: ใช้ Beer-Lambert Law
- **ความแม่นยำ**: ใช้ค่า R-squared

### การจัดเก็บข้อมูล
- ใช้โครงสร้าง JSON เดียวกับหน้าหลัก
- รองรับ Local Storage, Session Storage, Server
- Backup ข้อมูล Server ใน Local Storage

## 🎨 UI/UX Features

### Animations
- Pulse animation สำหรับการวัด
- Success/Failure animations
- Smooth transitions ระหว่างโหมด
- Hover effects บนปุ่มและรายการ

### Visual Feedback
- สีสถานะสำหรับ Real Time
- Color preview สำหรับ RGB input
- Loading spinners
- Responsive design

### Status Indicators
- **รอการวัด**: สีเทา
- **กำลังวัด**: สีเหลือง + Pulse
- **เป้าหมาย**: สีเขียว + Success animation
- **เกินเป้าหมาย**: สีแดง + Shake animation

## 🔗 การเชื่อมต่อกับหน้าหลัก

### จากหน้าหลักไป Prediction
```javascript
// ใน index.html
<button id="goToPredictionBtn">ไปหน้า Prediction</button>

// ใน script.js
function goToPredictionPage() {
    if (regressions.length === 0) {
        alert('กรุณาสร้างสมการเชิงเส้นก่อน');
        return;
    }
    window.location.href = 'prediction.html';
}
```

### การใช้ข้อมูลร่วมกัน
- ใช้โครงสร้าง JSON เดียวกัน
- รองรับการโหลดข้อมูลจากทุกแหล่ง
- แสดงสมการและค่าสถิติ

## 📊 ตัวอย่างการใช้งาน

### Real Time Analysis
```
เป้าหมาย: 6.0 mg/L
ความคลาดเคลื่อน: ±0.5
ผลการวัด: 5.8 mg/L
สถานะ: 🟡 ต่ำกว่าเป้าหมาย (เพิ่มขึ้น 0.2 mg/L)
```

### Normal Prediction
```
Input: Absorbance = 0.234
Output: Concentration = 4.567 mg/L
ความแม่นยำ: 98.7%
```

## 🛠️ การพัฒนาเพิ่มเติม

### ที่แนะนำ
1. **Real Hardware Integration**: เชื่อมต่อกับเครื่องวัดจริง
2. **Data Export**: ส่งออกผลการทำนาย
3. **Batch Processing**: ทำนายหลายตัวอย่างพร้อมกัน
4. **Advanced Analytics**: กราฟ Real Time, Trend Analysis

### การปรับแต่ง
- เปลี่ยนรหัสผ่าน Server ใน `prediction-script.js`
- ปรับความถี่การอัพเดท Real Time
- เพิ่มฟิลเตอร์สำหรับข้อมูลที่บันทึก

## 📝 หมายเหตุ

- ระบบ Real Time เป็นการจำลอง (Simulation)
- ข้อมูล Server ใช้ Local Storage เป็น backup
- รหัสผ่าน Server: `xquant2024` (เปลี่ยนได้)
- รองรับการใช้งานแบบ Offline

## 🔍 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย
1. **ไม่พบข้อมูล**: ตรวจสอบว่าบันทึกข้อมูลแล้ว
2. **สมการไม่ถูกต้อง**: ตรวจสอบรูปแบบสมการใน JSON
3. **Real Time ไม่ทำงาน**: ตรวจสอบการโหลดข้อมูล

### Debug
- เปิด Console (F12) เพื่อดู Log
- ตรวจสอบ Local Storage ใน Application tab
- ตรวจสอบ Network tab สำหรับ Server requests
